export async function register() {
  // Sentry initialization - uncomment when @sentry/nextjs is installed
  // if (process.env.NEXT_RUNTIME === 'nodejs' && process.env.NEXT_PUBLIC_SENTRY_DSN) {
  //   const Sentry = await import('@sentry/nextjs');
  //   Sentry.init({ dsn: process.env.NEXT_PUBLIC_SENTRY_DSN, tracesSampleRate: 0.1 });
  // }
}
