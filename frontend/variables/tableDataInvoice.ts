// TAJINE analysis invoice data - not currently used
type RowObj = {
  item: string;
  quantity: number;
  rate: number;
  amount: number;
};

const tableDataInvoice: RowObj[] = [];

export default tableDataInvoice;
